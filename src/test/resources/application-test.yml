spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/locadora_test
    username: postgres
    password: kl290499
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  test:
    database:
      replace: none
